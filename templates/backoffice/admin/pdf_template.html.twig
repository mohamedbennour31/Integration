<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Users List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .footer {
            text-align: right;
            font-size: 12px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Users List</h1>
        <p>Generated on {{ date|date('F d, Y H:i:s') }}</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.nomUser }} {{ user.prenomUser }}</td>
                    <td>{{ user.emailUser }}</td>
                    <td>{{ user.telUser }}</td>
                    <td>{{ user.statusUser }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        Total Users: {{ users|length }}
    </div>
</body>
</html>
